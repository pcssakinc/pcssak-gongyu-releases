# 2026-09-03 — 0.1.6 공개 README 후속 실측 안내 준비

## 작업 범위와 보존

- 공개 문서 동결 커밋 `39d96959db2a49822de27d454c0e973ddcb09dd0`에서 별도 `codex/v0.1.6-public-readme-closeout-20260903` 브랜치를 만들었다. 원래 게시 입력 작업트리와 동결 커밋은 변경하지 않는다.
- 작업 전 `CONTRIBUTING.md`와 최신 업무일지 2개를 읽었다. 이 공개 저장소에는 `AGENTS.md`와 `docs/CURRENT-STATUS.md`가 없어 기존 방식대로 새 업무일지에 진행 상태를 기록한다.
- README 한국어·영어에 승인 노트 작성 이후의 추가 실측 부록만 준비한다. 서명된 `docs/RELEASE-NOTES-v0.1.6.md`·법률 정본·공개키·지원 정책·태그·기존 자산은 수정하지 않는다.
- 최초 초안 준비 시점에는 0.1.6 실제 게시가 아직 완료되지 않았다. 당시 주 담당자는 최종 Actions 실행 `33726680848`의 성공을 확인하고 게시기 연결 판정 보정을 검증 중이었다. 이 문서 작업에서 Actions를 조회·실행·재시도하지 않았다.

## GitHub 공개 상태 후속 확인

- 주 담당자는 GitHub 릴리스 `381852770`, 태그 `v0.1.6`, 게시 시각 `2026-09-03T08:24:01Z`와 `draft=false`, `prerelease=false`, `immutable=true`, 자산 9종을 확인했다. 공개 대상은 기존 문서 동결 커밋 `39d96959db2a49822de27d454c0e973ddcb09dd0`다.
- 이 사실에 한정해 README의 공개 전 표현을 현재 GitHub 공개 상태로 바꿨다. 홈페이지 두 공식 도메인의 0.1.6 반영은 아직 진행 중이며 완료라고 표시하지 않는다.
- 이후 주 담당자는 게시기 종료 코드 0과 같은 릴리스의 불변 일반 Latest 상태, 정확한 9자산·두 서명·게시 후 재조회·설치본 내부 앱·기존 최종 Actions 실행 결속 검증 성공을 확인했다. 이 완료 신호를 받은 뒤에만 버전 고정 설치본 직링크와 Latest 표시를 추가했다.

## 추가 실측과 한계

- 주 담당자가 확인한 한 PC의 0.1.5→0.1.6 설치는 종료 코드 0, 16.452초다. 로그를 포함한 앱 데이터 전체 트리 비교가 일치하지 않아 모든 설정 파일이 동일하게 보존됐다고 확정하지 않는다.
- 사용자가 직접 Windows 계정 암호를 변경한 뒤 다른 PC에서 SSH 로그인에 성공했다고 보고했고, 주 담당자는 서버의 암호 인증 성공 1건도 확인했다. 기존 계정 암호를 앱이 생성·변경한 것은 아니다.
- 위 결과는 승인 노트 이후의 후속 관측으로 구분한다. SFTP·호스트 지문 대조·기존 세션 전체 종료·전체 Windows/물리 LAN 시험 행렬을 완료한 것으로 확대하지 않는다.
- 암호·계정명·IP 주소·개인 경로·개인키·호스트 지문·원본 로그는 문서에 기록하지 않는다.

## 검증과 다음 단계

- 한·영 README의 수치와 한계 표현을 맞췄다. `git diff --check`가 통과했으며 변경은 README 2개와 이 새 일지뿐이다. 서명된 승인 노트·법률·지원 보안 정책·스키마는 동결 커밋과 동일하다.
- 실제 게시 ID·시각·불변 Latest와 최종 자산 검증 성공을 전달받아 GitHub 공개 상태·고정 설치본 링크만 갱신했다. 홈페이지는 진행 중 표시를 유지한다. 홈페이지 반영은 주 담당자의 후속 확인 결과로 별도 기록한다.
- 주 담당자가 검토 뒤 로컬 커밋·작업 브랜치 푸시·Draft PR 생성을 승인했다. 병합은 승인 범위에서 제외한다. 이 작업에서는 빌드·서명·Actions 실행·제품 게시·실제 Windows 설정 변경을 수행하지 않는다.
- 배포 입력 동결 후의 사용자 실측은 새 부록으로 기록하고, 서명된 승인 노트나 자산을 수정해 같은 버전의 검증을 반복하지 않는다.
